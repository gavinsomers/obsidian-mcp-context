name: obsidian_mcp_context
version: "0.3.0"
config-version: 2

profile: obsidian_mcp_context

model-paths: ["models"]
test-paths: ["dbt_tests"]

models:
  obsidian_mcp_context:
    +materialized: table
    staging:
      +schema: staging
      +materialized: view
    intermediate:
      +schema: intermediate
      +materialized: view
    marts:
      +materialized: table
      dim:
        +schema: dim
        +materialized: table
      fact:
        +schema: fact
        +materialized: table
      mart:
        +schema: mart
        +materialized: table
